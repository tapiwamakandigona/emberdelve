<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- v0.3.4: needed to drive the Vibrator service directly for the in-game
         Haptics toggle. Normal install-time permission. -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Telemetry: opt-in Firebase Analytics only (docs/telemetry-events.md). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Daily Delve reminder (v0.6.0, opt-in): re-register the scheduled
         slots after a device reboot. POST_NOTIFICATIONS itself merges in
         from the flutter_local_notifications plugin manifest and is only
         requested at runtime when the player flips the Settings toggle. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <!-- Never collect the advertising ID (no ads, no ad personalization).
         The Play measurement SDK also merges in the Privacy Sandbox
         AdServices permissions; this game does no ads, attribution or
         conversion measurement, so all three are force-removed. Verify with
         `aapt2 dump badging` after touching this block. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION"
        tools:node="remove"/>
    <!-- Backup + device migration (Feb-2027 Play quality requirement).
         The paid Forge unlock (emberdelve_meta.json) is explicitly INCLUDED
         in cloud backup and D2D transfer — owner decision, DEMAND.md
         "Owner directive 2026-08-31b". allowBackup is true on purpose;
         the explicit include lists in res/xml/ are the source of truth. -->
    <application
        android:label="emberdelve"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Analytics collection is OFF until the player opts in at runtime
             (TelemetryService.setAnalyticsConsent). Prominent-disclosure
             consent model; see docs/telemetry-events.md. -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="google_analytics_default_allow_ad_personalization_signals"
            android:value="false" />
        <!-- Play Games Services v2 (v0.5.0): the PGS project id from
             Play Console > Grow > Play Games Services > Configuration. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id" />
        <!-- Kill PGS auto sign-in: connecting is an explicit opt-in tap in
             Settings (same consent charter as analytics). The SDK is instead
             initialized manually in MainActivity.onCreate. -->
        <provider
            tools:node="remove"
            android:name="com.google.android.gms.games.provider.PlayGamesInitProvider"
            android:authorities="com.tsorostudios.emberdelve.playgamesinitprovider" />
        <!-- Daily Delve reminder (v0.6.0): flutter_local_notifications
             delivery + reboot re-registration receivers. Both are internal
             (not exported) and inert unless the player opted in. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
