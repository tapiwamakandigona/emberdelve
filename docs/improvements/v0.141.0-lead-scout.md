# v0.141.0 lead scout — The Honest Ledger

Trail: v0.140 proved one shelf-badge had rotted; rot is never
alone. A regex sweep of every honor whose copy says every/all/
whole against live catalog sizes found two more, both from the
era before the re-pricing doctrine (v0.119).

Choices:
- Pin the CLASS, not the instances: the new test asserts all
  eight promise honors against their catalogs, so the next
  catalog growth FAILS CI until its promises move — the doctrine
  is now enforced, not remembered.
- Count-free copy guard: both bugs were baked-in numbers; the
  test forbids number words in promise text outright.
- Test-data lesson: hand-copied catalog lists in maxed profiles
  hide exactly these bugs — derive from the catalog.
