# v0.140.0 lead scout — The Kept Hearth

Trail: the ember-sink audit pointed at the theme shelf (untouched
since v0.4.3) — and reading the catalog against its badge exposed
the real find: hearth_keeper's target froze at 4 when the shelf
was 4, and nobody moved it when v0.4.3 tripled the catalog. The
re-pricing doctrine (v0.119) did not exist then; this is exactly
the class of bug it prevents.

Choices:
- Fix by MOVING the target (promise wording), not renaming: the
  badge's earned-state derives at read time, so profiles that
  'had' it at 4 simply see honest progress again — nothing
  persisted is destroyed.
- Junk-proof the reader while touching it (the v0.127 idiom is
  now the house style for every catalog stat).
- Two colours, not four: the ladder tops at 480; a deeper batch
  would outrun the ember economy's earn rate for no design gain.
