# v0.100.0 lead scout — The Second Cycle

Lead (queued): more hearth tales past the first cycle. Scouting it
surfaced a latent regression in my own v0.98.0 design: a gate that
follows a growing list takes back earned rewards. The fix (frozen
milestone const) shipped WITH the growth that would have triggered it,
and the release notes say so plainly — honesty is the brand, including
about our own code.

Copy choices: second cycle = character studies (one per delver,
matching the six-delver roster fact from tale 5) then world texture.
No tale states mechanics numbers — tips and counted surfaces own
numbers; tales own meaning.

Queued leads: Ledger filter by delver; share-card polish round 2
(trace grid glyph weight).
