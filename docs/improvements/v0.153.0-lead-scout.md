# v0.153.0 lead scout — The Shared Fire

Trail: retention audit of the teaching layer. Tips taught the
run (map, dice, intent, combos, block, anvil) but nothing taught
the RETURN — the daily is the reason to come back tomorrow, and
it sat behind an unexplained button.

Choices:
- Title moment, not run moment: first contact with the shared
  delve is the button itself. This is the deck's first out-of-run
  tip; the direction (director stays pure, screen passes real
  counters) held without change.
- The dailyPlayed guard matters more than the wonBefore one: a
  tip explaining a thing the player already does reads as the
  game not paying attention.
- StatefulBuilder over making dismissTip notify: dismissTip is
  called from setState contexts in three run screens — adding a
  notify would rebuild routes mid-animation there.
