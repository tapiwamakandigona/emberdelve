# v0.128.0 lead scout — The Smith's Shelf

Trail: the open-lanes map (v0.126.0) lists cosmetics as a zero-
anchor lane, and the sink audit showed the wardrobe frozen since
v0.94.0 while ember income grew (provings, weeklies, codex refunds
none — embers accumulate). A healthy sink keeps the earn loop
meaningful; the forge theme keeps the batch coherent with the
v0.124–0.126 arc.

Choices:
- Gap-driven picks (dark dye, steel + violet dice) over more warm
  variants: each entry earns its shelf slot by being visibly NEW.
- Prices extend the ascending ladders (640 / 500+550): the ladder
  IS the guard — attire_test enforces it, no judgment call.
