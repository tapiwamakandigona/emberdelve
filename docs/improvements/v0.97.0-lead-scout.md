# v0.97.0 lead scout — The Counted Forge

Lead: the honest-numbers arc stopped one screen short. Rest, shop heal,
and event options all print real numbers; forge rows still said only
names. Scout found `_dieDesc` already rendering honest die facts on the
reward screen (shared silently by shop and boon pickers via the parts
mechanism) — the fix was promotion, not invention.

Self-critique in plates: first cut printed 'd6 → d8' under a plain
forge — three statements of one fact (chips + labels + facts line).
Added the restraint rule: facts only when mods exist on either side.

Temper sheet scouted and left alone: it already explains each rune
per-row and states the once-per-delve cost twice.

Queued leads carried forward: more vistas, share-card polish.
