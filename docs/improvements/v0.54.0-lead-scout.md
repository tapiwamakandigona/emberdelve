# v0.54.0 lead scout — 2026-08-25

Written while v0.53.0 CI runs. Ranked candidates, same discipline:
one improvement, honest, zero-or-provable sim impact.

## 1. The Obituary's second act — epitaph on the Delver's Card (LEADING)

Deferred twice for surface-fatigue (v0.52 scouting) and now one full
release of distance from v0.51.0. The share card (lib/ui/share_card.dart,
DelverCard/DelverCardFacts) gains the run's 2–3 sentence story under the
name line — the card becomes a shareable artifact that carries a NARRATIVE,
not just numbers. Hook logic: obituary text is the thing players paste in
chats (v0.51.0 shipped copy-as-text); putting it on the image closes the
loop for image-first channels (WhatsApp statuses, stories).

- `obituaryText` (lib/game/obituary.dart) is already pure and sim-free.
- Risk: MEDIUM (layout) — the card is size-constrained. May need a 2-line
  clamp variant (`obituaryText(short: true)` or maxLines+ellipsis; prefer
  an honest short VARIANT over mid-sentence ellipsis).
- Tests: pin card shows the epitaph for a played-out win and loss; clamp
  behavior pinned. Plates: card at all 4 sizes, win + loss variants.
- Zero sim changes by construction.

## 2. A fifth vista — earned, provings-tied

Reviewer ask ("change backgrounds") shipped as 4 vistas in v0.35.0; a
player who cleared the early milestones has nothing left to earn there.
A fifth VistaDef (data + one `vistaUnlockedFor` case reading a real
counter — provings cleared count fits: no vista reads it yet) is cheap
and connects two systems. Palette-only, zero PNGs, zero size growth.
Risk: LOW-MEDIUM (taste — needs plate critique of the grade over real
scenes). Candidate: deep violet-gold "Duskquartz", unlock "Clear 3
provings."

## 3. Rumor follow-through — boss-aware map whisper

The v0.53.0 rumor names the boss at run start; a one-line callback when
the player reaches the final layer ("The <boss> is close.") would close
the arc. SMALL, but touches the map screen (320px real estate measured
tight in v0.51 scouting) and risks noise. Hold until there's evidence
players want more of the rumor voice.

Recommendation: build #1 next.
