# v0.151.0 lead scout — The Tenth Way

Trail: the standing recipe — every delver release is followed by
its proving + honors release. Nothing to decide except the seed
(9: first mender normal win not already used by a neighbor) and
the honor names (The First Stitch: the mend fiction; Ten Ways
Down: the family's fixed naming).

Reminder that keeps biting: the provings screen header pin
('N of 18') lives in provings_test in TWO places (locked list +
cleared mark) — the count pin at the top of the file is not the
only one.
