# v0.149.0 lead scout — The Tenth Fire

Trail: ranks.dart was the last unexplored data catalog. Audit
found a dead top: the formula grows forever, the ladder didn't —
a retention hole at exactly the players most invested (endgame
progression is the classic long-tail hook; the studio-research
notes from the v0.9x sessions flagged it).

Choices:
- ONE tier, not two: gaps double-ish per step; a 2600 tier today
  would be a promise nobody reaches this year. Add the next rung
  when veterans near 1700.
- Above Sovereign, not inserted below: insertion re-names held
  ranks; extension only adds a climb. Monotone under merge either
  way, but extension is also monotone in NARRATIVE.
- New curve pin (gaps never shrink) so future tiers keep the
  shape without re-deriving the tuning by hand.
