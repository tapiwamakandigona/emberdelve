# v0.51.0 lead scout (written during v0.50.0 CI wait, 2026-08-25)

Roster is now 6; recent releases: format (Short Road), character (Tinker).
Under-served axes, ranked:

1. **Run-end storytelling** — the death/win screen is the most re-seen
   surface after the title. A "delve obituary": where you fell, what die
   betrayed you, the relic that almost saved you — shareable text, feeds
   the same hooks as Delve Codes. Cheap (app-layer only, run trace already
   exists), high retention surface. LEADING CANDIDATE.
2. **Tinker-aware content ripples** — epithets/provings/waymarks referencing
   the sixth delver keep older systems feeling alive after a roster grow
   (a proving for the Tinker mirrors the Ninth Proving precedent).
3. **Boss telegraphs pre-fight** — map already telegraphs rewards; a boss
   "rumor" at run start (which crown waits below) aids build planning.
   Sim-adjacent: rumor = read-only of existing map data, no golden risk.

Constraint reminder: one improvement per release; sim changes need sweep
bands + golden re-anchor justification. 1 and 3 are app-layer only.

## Obituary groundwork (verified against code, 2026-08-25)

- `_runRecord` (controller.dart ~L1227) has NO death cause today — only
  floor/floors/result/seed. The killer is still readable at summary time:
  terminal sim keeps `sim.enemy` on run_lost (name/id/boss/elite), plus
  `sim.turn` for "how long the last stand lasted".
- RunTrace marks (clean/hurt per floor) already narrate the road; the
  obituary composes: killer line + floor line + the run's hurt profile +
  first-felled (summary_screen.dart ~L788 already computes first fellings).
- Share text precedent: dailyResultShareText (controller.dart) — same
  plain-text copy pattern, no image pipeline needed for the first cut.
- Zero sim changes; `_runRecord` may gain `killed_by` (additive, ledger
  tolerates unknown keys — verify cloud_merge passthrough before assuming).
