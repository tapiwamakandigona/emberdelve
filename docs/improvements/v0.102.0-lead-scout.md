# v0.102.0 lead scout — The Painted Trace

Lead (queued): share-card trace-grid glyph weight. Scouting reframed
it: the glyphs' WEIGHT was never ours to set — emoji cells are drawn
by the platform's emoji font, so the exported card was vendor-styled.
The fix is not a better glyph but ownership: paint the grid.

Choices:
- Parse the banked emoji string rather than re-derive from RunTrace:
  records store toCompact + outcome since v0.57.0, and remembered
  cards must repaint without recomputation.
- Honest semantics: the outcome cell overwrites its floor's mark, so
  the label states floors + outcome, never per-floor counts.
- Share TEXT unchanged — emoji is correct where there is no painter.

Test lesson (banked in harness memory): a SemanticsHandle from
tester.ensureSemantics() must be disposed IN the test body —
addTearDown runs after the framework's end-of-test verification and
fails the test.

Queued leads: per-delver win tallies on the Delver's Page if it
proves used; summary screen's on-screen trace could someday paint
too (kept: in-app emoji is consistent per device, low stakes).
