# v0.56.0 lead scout — 2026-08-25

Written while v0.55.0 CI runs. Same discipline: one improvement, honest,
zero-or-provable sim impact.

## 1. Card from the Ledger — share a REMEMBERED delve (LEADING)

Deferred from the v0.55 scout for a release of distance from the card;
v0.55.0 was a vista, so the distance exists. The Ledger keeps 30 run
records (date, character, difficulty, ascension, result, floor, floors,
seed, embers, short?, daily?, killed_by since v0.51.0) and already
rebuilds honest Delve Codes from them — but the Delver's Card dies at the
summary screen: the "let me show you the one that killed me" moment is
gone the next morning.

- Build DelverCardFacts from a record. Honesty budget per field:
  - won/delver/difficulty/ascension/embers/seed/code: straight from the
    record (code via the Ledger's existing rebuild, short-aware).
  - epitaph: loss + killed_by → the exact epitaphLine loss form (floor is
    in the record). Loss without killed_by (pre-v0.51 record) → opener
    only, floor included. Win → boss = bossForSeed(seed) (deterministic,
    proven by The Rumor) → the win form.
  - fightsWon + trace grid: NOT in records → omit. Card render must learn
    to drop the fights line and trace block when absent (degrade by
    omission, never invention — no zeros pretending to be facts).
  - epithetTitle: worn-at-the-time is unknowable → omit ('' already
    supported).
- Entry point: a share icon on each Ledger row → the existing card sheet.
- Risk MEDIUM: card render layout with omitted blocks needs its own
  worst-case widget test (Ahem + realistic-max facts, both with and
  without the optional blocks) + plate critique; zero sim changes.

## 2. Duskquartz follow-through — provings shelf hint (HOLD)

The provings screen could name the vista reward on locked rows. Small,
but the shelf already states "Clear 3 provings." — wait for evidence
players don't find it.

## 3. Rumor map whisper — HOLD, unchanged.

Recommendation: build #1.
