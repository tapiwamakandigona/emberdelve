# v0.106.0 lead scout — The Cold Camps

Audit pass first (the queued lead): hardcoded-count sweep (all
player-facing counts derive from catalogs — clean), cloud_merge
field diff vs MetaState.toJson (only 'schema' absent, correct —
merge works on objects, fromJson owns schema), character picker
already carries per-delver tallies (v0.60.0). No dishonesty found.

Retention doc (2026-08-11) re-read: P0–P5 all shipped; its P2
content targets long exceeded (17→42 enemies, 16→46 events). The
one thin axis left: the Weekly's rule pool — 4 rules = monthly
repeat. Growing the pool is honest variety on an existing hook.

Rule chosen: no_rests over alternatives (all_d8 'bounty week'
rejected: positive-variance weeks read as rigged generosity;
hp-tax rules rejected: combat surgery for a map-flavored idea).
Cold Camps reuses the proven pure-relabel seam, and winsNoRest is
already a known concept from achievements.

Sweep before shipping: 150 seeds — 97/150 plain vs 99/150 no_rests
for the bot (fight embers offset lost healing). Humans rest more
than the bot, so the felt difficulty is higher; acceptable ladder
placement between no_shops (65%) and all_d4 (20%) extremes.

No plate: zero new UI surface — the rule flows through the already-
plated weekly card, in-run badge, and ledger marks via the catalog.
Mechanics fully test-covered (relabel diff, composition, rotation).

Queued leads: Gramophone place entry if tracks grow again; a
'double week' (two composed rules) once the pool feels roomy —
the compose loop already supports it by design.
