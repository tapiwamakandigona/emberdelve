# v0.132.0 lead scout — The Second Mark

Trail: the v0.130 lane note — rune AXES full, next temper release
should be depth. Depth options weighed:
- Second temper (CHOSEN): multiplies build space (6 picks → 21
  pairs) for a one-line sim change, zero re-anchor by bot policy.
- Rune upgrades (rejected for now): needs new persisted rune
  tiers in custom_dice — a save-format change for a narrower win.
- Temper in shops (rejected): moves the anvil off the rest screen
  and dilutes 'the hollow is where you shape things'.

Balance reasoning: a rune triggers ~1/6 of that die's rolls; a
second rune adds about one trigger every few fights — meaningful,
not warping. Hard mode's margin (+150 HP effective) dwarfs it.
If sweeps later show drift, the knob is the cap, one line.
