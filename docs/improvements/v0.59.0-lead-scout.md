# v0.59.0 lead scout — 2026-08-25

Written while v0.58.0 CI runs. The remembered-delves arc (v0.54–v0.58:
epitaph → card → fuller record → rows) is mined out for now — five
releases on one hook family is enough; rotate.

## 1. The Proven — a tenth epithet for clearing every proving (LEADING)

The Provings arc has a floor reward (duskquartz vista at 3 clears) but no
summit reward: clearing all 10 provings changes nothing but the counter.
Epithets are the identity system, and identity is what a completed
gauntlet has earned.

- New epithet `the_proven`, title "the Proven", unlock "Clear every
  proving." — stat `provings_cleared`, target = provings count (10). Data
  + meta only; `meta.provingsCleared` is already a Set the achievements
  resolver can count.
- Check: epithets order append-LAST; the epithets shelf is a lazy list —
  the new card needs an ensureVisible in plates/tests (vista-shelf
  lesson). Worst-case card/row text: "the Proven" is SHORT (9 chars) —
  no overflow risk beyond existing worst cases ("the Well-Oiled" stays
  longest).
- With v0.57.0 fuller records, the new epithet immediately flows into
  Ledger rows and remembered cards — the systems compound.
- Risk LOW: zero sim; needs epithet-shelf plate + waymarks/achievements
  interaction check (does the epithet system surface progress? verify
  how existing target-N epithets like the_exact display partials).

## 2. An eleventh proving (mutator-based) — MEDIUM

Provings are sim-SAFE content (fixed seed + mutators, no base-sim
movement — the Ninth and Tinker's Provings shipped without re-anchors)
but need an offline seed hunt (hunt tool pattern:
tool/tinker_proving_hunt_test.dart). Good candidate when a distinct
mutator idea earns its place; don't ship a re-skin. NOTE: if #1 ships
first, target stays "every proving" only if the unlock reads the LIVE
provings count, not a hardcoded 10 — design it that way regardless.

## 3. Rumor map whisper — HOLD (no demand evidence for more rumor voice).

## 4. Provings shelf vista hint — HOLD (no evidence players miss it).

## 5. Marketing note (not a release): an itch.io devlog covering the
   remembered-delves arc (v0.54–v0.58) is due — the last devlog covers
   v0.26.0-era content. Draft after v0.58.0 publishes.

Recommendation: build #1.
