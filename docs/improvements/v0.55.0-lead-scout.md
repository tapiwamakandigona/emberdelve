# v0.55.0 lead scout — 2026-08-25

Written while v0.54.0 CI runs. Same discipline: one improvement, honest,
zero-or-provable sim impact.

## 1. Duskquartz — a fifth vista, provings-tied (LEADING)

Deferred from the v0.54 scout, now first in line. The vista shelf (v0.35.0,
the reviewer's "change backgrounds" ask) is a finished ladder: a player who
won a delve, felled 15 foes, and cleared Hard has nothing left to earn
there. Meanwhile the Provings (10 puzzles) feed NO cosmetic — the two
systems never touch. One VistaDef + one resolver case connects them:

- `duskquartz`: deep violet-gold grade ("Quartz veins catch the last light.
  The delve keeps its own dusk."), unlock "Clear 3 provings."
- `vistaUnlockedFor` gains a `provingsCleared` param (int) — callers pass
  `meta.provingsCleared.length`; derived at read time like the rest, so
  nothing persists and the unlock can never lie.
- Append-LAST to vistasOrder (same discipline as roster bits).
- Palette-only: hue/sat/val + wash, zero PNGs, zero APK growth.
- Risk: LOW-MEDIUM (taste) — plate critique of the grade over real map,
  combat, and hearth scenes at 4 sizes; tune hue/wash until it reads
  "dusk," not "bruise."
- Tests: resolver truth table (0/2/3 provings), vista sheet shows the
  locked line, unlock flips with a real cleared set; existing vista tests
  keep passing untouched (proves no default drift).

## 2. Card from the Ledger — share a REMEMBERED delve

The Epitaph (v0.54.0) makes the card a story; the Ledger (Remembered
Delves) already keeps runHistory with `killed_by`. Building DelverCardFacts
from a ledger record lets a player share LAST week's run — the "let me show
you the one that killed me" moment currently dies at the summary screen.
MEDIUM: needs an honest epitaph from a record (no live sim — floorReached
and trace come from the record; older records lack fields → degrade by
omission, never invention). Strong hook, second only because the card just
changed and deserves a release of distance before growing again.

## 3. Rumor follow-through — boss-aware map whisper (HOLD, unchanged)

Still waiting on demand evidence for more rumor voice; map 320px real
estate still tight.

Recommendation: build #1.
