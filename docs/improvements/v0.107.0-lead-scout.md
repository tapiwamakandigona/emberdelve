# v0.107.0 lead scout — The Unwritten Feats

Trail: tracks (8) are asset-bound — growing music needs real audio,
not a data edit, so the Gramophone place entry stays queued. The
achievements catalog, though, stopped at v0.59.0's provings while
six systems shipped after it with zero recognition. The retention
doc's P0 finding (metric achievements are the strongest honest
lever, difficulty rising monotonically with retention) applies to
NEW content exactly as it did to old.

Choices:
- codex_forty target 40, not codexEntries.length (86): a
  length-bound target goes stale the day the catalog grows —
  all_three_bosses taught that lesson on arrival. Fixed targets,
  catalog-bounded by test instead.
- distinct_felled junk-proofed through the enemies catalog;
  tales_heard reads the plain counter (hearthTalesHeard has no
  catalog identity — it is a count, and the modulo wrap means
  20+ heard is legitimate).
- No tracks_heard feat: an 8-of-8 target would freeze the catalog
  or go stale; the Gramophone already shows N of M itself.
- Recognition only — no ember grants (the charter's 'earning one
  can never make the next run easier').

Queued leads: Gramophone place entry if tracks grow; 'double week'
composed rules once the pool feels roomy; a feats plate pass if the
achievements section ever changes widgetry.
