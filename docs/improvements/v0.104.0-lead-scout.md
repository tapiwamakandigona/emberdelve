# v0.104.0 lead scout — The Delve Itself

Lead: the last unserved item of the planned arc (lore drip), aimed by
a verbatim review line — "I still don't understand what's a delve."
Audited the codex: enemy and relic coverage complete (v0.95.0 icon
pairing also complete, checked), but the world itself had no page.

Choices:
- A third codex kind over a free tutorial screen: the codex is the
  established home of lore, and the tales already drip world-words
  free at rests — the codex is the collected, priced deep-dive.
  Nothing mechanical is in these entries, so the charter holds.
- Places shelved FIRST and priced CHEAPEST (10): the newest player's
  question should be the easiest to answer. Order inside the shelf =
  the order the questions get asked.
- placeNames lives in codex.dart (places exist nowhere else); enemy/
  relic names stay read from their own catalogs (rename honesty).
- Summary trace stays emoji ON PURPOSE (queued lead closed): the
  charter there is 'the share artifact IS the display' — the on-
  screen grid must be exactly what pastes. The card paints because
  it never pastes.

Queued leads: per-delver win tallies on the Delver's Page if it
proves used; a place entry for The Gramophone if tracks grow again.
