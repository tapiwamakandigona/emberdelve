# v0.139.0 lead scout — The Shown Anvil

Trail: the onboarding audit (after two false scouts — summary
waymark and reduce-motion both already existed; grep before
scouting). The tip deck froze at v0.10.0's five combat cards;
every system since (temper, weekly, codex) relies on discoverable
buttons. The anvil is the one whose PAYOFF is invisible until
tried — a button labeled 'temper a face' explains nothing about
why you would.

Choices:
- The rest fire, not the sheet: teach at the moment of choice,
  not after commitment (the intent_fair 'receipt moment' rule
  inverted — this one is a menu moment).
- All six runes in one sentence over a rune-by-rune tour: the
  sheet already details each; the card only has to make the
  system legible enough to open.
- Weekly tip rejected for now: the weekly block is self-labeled
  on the title screen and gated behind forgeUnlocked — its first
  contact already comes with copy.
