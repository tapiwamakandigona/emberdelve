# v0.93.0 — The Pictured Satchel

Relics carry hand-drawn icons that build recognition at the shop — but
the in-run inventory, the one place a player reviews everything they own,
listed bare text. Recognition earned at the stall was thrown away in the
satchel.

- Every inventory row now leads with its relic's icon (36px — dialog rows
  are denser than shop panels), vertically centered across name and
  effect text. Starting-relic tag, pickup order, and the empty state are
  unchanged.

## Technical

- relic_inventory.dart row restructure only; zero sim movement. Existing
  test/relic_inventory_ui_test.dart extended: the row's AssetImage is
  asserted present. Plate-critiqued at 360x640 and 320x568 with a
  five-relic satchel (tool/pictured_satchel_visual_test.dart).
- Found while plating (queued): cairn_stone and choir_censer share the
  fallback lantern icon in the relicIcons map — distinct art is a future
  lead. Plate harness lesson recorded: dialog asset icons need
  build-then-delay-then-repaint or cold-cache rows snap blank.
