# v0.158.0 — The Shieldwright

The eleventh delver: the WARD. The first kit to lead with a DEEP mark
(v0.155.0) — an Aegis II already worked into an Ember Die's 6, the
shield forged before the first stair.

- **Kit:** 26 HP, three Ember Dice, no relic — the depth of the mark,
  not the pouch, is the kit. Smith's deep temper: Aegis II on the
  first d6's 6. Unlock: 1350 embers.
- **Balance** (400-seed bot sweep vs kindler 89.75/67.25/41.50):
  **26 HP 89.00/68.00/41.50 SHIPPED** — hard exactly at kindler par,
  normal +0.75 (between bearer 67.5 and kindler); in band first try —
  a single deep face on one die of three is modest, steady tempo.
- **Engine:** `startTempers` entries accept an optional `tier` key
  (same contract as the save shape: absent = tier 1), so the
  runesmith's and every earlier record stays byte-identical — pinned
  by test.
- **Fallout wired:** tales 5 + 40 ('Eleven keep this fire' / 'Eleven
  chairs'), codex 11th delver page (114 entries), promise honors moved
  with the roster (full_company, crowned_company, the Many-Handed →
  11), Planishing Hammer weapon (reach 0.50, deep-steel-blue accent),
  shieldwright.png sprite (warden remap: hue +215°, sat ×0.95 floor
  0.38, val ×0.92 — cobalt steel vs the bearer's desaturated granite,
  same source sheet), PROVENANCE + sprite_meta rows.

## Technical

- test/shieldwright_test.dart (4): def + sim application pins (tier
  KEY present in the custom-die record), tier-1 smith's marks still
  write NO tier key (runesmith byte-identity), smith's-mark-isn't-
  forge-work guard, seeded win AND loss pins (easy loss 3; normal
  wins 1/2/4/6/9/10/11).
- Roster-count pins updated (runesmith_test, fourth_cycle_test).
- Plates: tool/shieldwright_visual_test.dart (warmup + 2 sizes).
- Proving + shieldwright_wins + eleven_ways_down follow in v0.159
  (the v0.119 delver→proving standing recipe).
