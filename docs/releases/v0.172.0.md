# v0.172.0 — The Fourteenth Way

The collier's proving and roster honors (the v0.119 pattern, eighth use).

## The proving

- **The Collier's Proving** — the Collier on normal floors, seed 6
  (bot-winnable, pinned by test). Blurb teaches the kit's core read:
  every die arrives worked — the blade cuts, the gilt pays, the mend
  holds — nothing to build, everything to spend well.
- Stands directly after the Cutler's Proving: delver provings stay
  together, in roster order. `ash_summit` remains last. 22 provings.

## The honors

- **The First Coal** — win a delve as the Collier (`char_wins`, param
  `collier`, target 1).
- **Fourteen Ways Down** — win a run with fourteen different delvers
  (`delvers_cleared`, target 14).
- Frozen predecessors keep their earned prices: Thirteen Ways Down
  stays 13, Twelve Ways Down stays 12 (pinned).
- **the Proven** moves 21 → 22 with the list, per the target-honesty
  pin (`the_proven.target == provings.length`).

## Tests

- New `test/fourteenth_way_test.dart`: proving placement/params, seed
  6 bot-win on normal, honors pattern + frozen predecessors.
- Count pins moved 21 → 22 in `provings_test` (x2 + '0 of 22' /
  '1 of 22' copy) and `eighth_way_test`.
- Full suite 1079/1079; analyze clean (rebuild-cost lints on).
