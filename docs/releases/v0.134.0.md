# v0.134.0 — The Runemark

The tenth vista, closing the Six Marks arc at every reward tier a
completed collection earns (badge v0.133, and now the world):

- **Runemark** — the first VIOLET grade: runeglass light, cool and
  arcane where forgelight burns. Hue +155 / sat 1.35 / val 1.06 /
  wash 0x5C7B4FC0, opened bold per the grade doctrine (frostvein
  and forgelight both shipped 'too timid' first passes; this one
  did not).
- Unlock: temper every rune the anvil offers. The GATE is frozen
  at six (a gate tracking the live rune set would re-lock an
  earned vista if the anvil grows — the v0.100 hearthgold lesson);
  the six_marks BADGE keeps the promise wording and moves.

## Technical

- vistaUnlockedFor gains required runesMarked (junk-proofed at the
  controller: faceRunes filter). 17 call sites patched by scripted
  regex on the 'tempersSet: N,' lines (the v0.126 patch pattern).
- forgelight_test's 'last' pin -> index pin (FOURTH occurrence of
  the catalog-growth 'last' bug; vistasOrder joins the grep list).
- test/runemark_test.dart (3): slot/grade honesty, resolver flips
  at exactly six, controller gate junk-proof end to end.
- Plates: violet map vs emberlight control + shelf card worn and
  locked. PLATE-CLONE FILENAME lesson re-confirmed: capture-name
  strings need their own rename pass (longer tokens than the id).
