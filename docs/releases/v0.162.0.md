# v0.162.0 — The Gilder

The twelfth delver takes a chair: **The Gilder**, the GOLDSMITH — the
first kit with **two smith's marks**. Gilt is worked into both Ember Dice
sixes before the first stair, so the pouch itself pays: coin on the roll,
not on the kill (the peddler's opposite pole — kiln gold arrives per fight
won, gilt gold per gilded face).

## Kit

- 28 HP · `['d6', 'd6', 'd6']` · no relic — the twin marks ARE the kit.
- `startTempers`: gilt (tier 1) on die 1 face 6 AND die 2 face 6 — the
  first multi-entry `startTempers` shipped; the v0.135.0 loop applied it
  unchanged (both marks are the smith's: `tempers_used` 0, Six Marks bank
  untouched, temper cap intact).
- Unlock: 1500 embers (ladder stays ascending after shieldwright's 1350).
- Delve-code index 11 (bits 31..34) — appended LAST, every shared code
  stays stable.

## Balance

400-seed bot sweep ×3 difficulties vs kindler (89.75/67.25/41.50):

| HP | easy | normal | hard |
| --- | --- | --- | --- |
| 30 | 90.50 | 67.50 | 43.50 |
| **28 (shipped)** | **90.25** | **65.50** | **41.50** |

In band: slightly under on normal like the other skill delvers, hard at
par. The bot undervalues shop gold, so the human-facing kit errs lean.

## Art & wiring

- Sprite: palette variant of the peddler sheet (hue +230°, sat ×1.00
  floor 0.38, val ×1.05 — burnished gold; PROVENANCE row added), so the
  two coin-delvers share a silhouette and nothing else. sprite_meta.json
  entry mirrors the peddler rows (idle 4f / run 4f / hit 1f).
- Weapon: Agate Burnisher (short reach, burnished-gold accent).
- Codex delver entry; tales 5 & 40 recounted ("Twelve keep this fire",
  "Twelve chairs") per the v0.120.0 honesty edit contract.
- Roster promises moved with the roster (v0.140 audit rule):
  `full_company` 12, `crowned_company` 12, `the_six_handed` 12.
- Frozen gates did NOT move: tenthfire vista stays `charsUnlocked >= 10`
  (tripwire reason updated only), five…eleven_ways_down unchanged.
- Tests: `test/gilder_test.dart` (kit pin at index 11, both marks write
  tier-free records, smith's-work-not-player's, seeded win/loss replay
  pins); roster count pins updated (12) in fourth_cycle, runesmith,
  tenth_chair tests.

The gilder's proving, `gilder_wins` achievement, and `twelve_ways_down`
follow in the next release (the v0.159.0 pattern).
