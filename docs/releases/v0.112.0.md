# v0.112.0 — The Frostvein

The eighth vista, and the first the Weekly feeds: win the Ember on a
doubled week (v0.111.0) and the cold stays in the stone.

- **Frostvein** — the first PALE grade: frost drains color the way
  the deep does, but it brightens. Ice-blue veins in white-lit rock;
  nothing like moonveil's saturated night or deepshale's warm dark.
- Earned on the rotation's hardest sit: claim the Ember under Cold
  Quarter. The unlock line says exactly that — vistas are milestones,
  never purchases (§Ethics).
- Fed by a new monotonic counter (`doubledWins`) — the hearthgold
  freeze (v0.100.0) taught that a derived gate which can re-lock is
  a bug; a counter that only climbs can never take a vista back.

## Technical

- meta: `doubledWins` (persisted only when > 0, so old saves are
  byte-identical), cloud merge takes the max.
- Banking: a WON weekly whose label holds a '+'-joined pair climbs
  the counter.
- vistas.dart: 'frostvein' appended LAST (roster-bit discipline);
  resolver gains required `doubledWins`.
- Fixed latent test hazard: marked_week_test handed the raw weekly
  label to botCmd — on a real doubled week that becomes one bogus
  id. Split on '+' (the coming Monday, 2026-08-31, IS doubled).
- test/frostvein_test.dart (5): catalog/gate, a driven doubled-week
  WIN (2026-02-02, hunted bot-winnable) banks + opens + announces,
  singles never count, persist/merge, honesty sweep. Plates: map vs
  emberlight control + locked/worn cards approved.
