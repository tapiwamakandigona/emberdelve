# v0.152.0 — The Tenth Chair

The full company's vista. With ten delvers on the roster
(v0.150), chasing all ten needed a reward you can SEE:

- **Tenthfire** (11th vista): warm amber grade (hue +24, sat
  ×1.25, val ×1.10, ember-gold wash) — the delve in the light of
  a full fire. Unlock: every delver at the fire.
- Gate FROZEN at ten (the v0.100 lesson): a gate that tracked
  the live roster would re-lock this vista the day an eleventh
  delver joins. The test carries a tripwire pin that says so.
- Resolver stays pure/derived (§Ethics): one new counter
  (charsUnlocked) threaded through vistaUnlockedFor.

## Technical

- test/tenth_chair_test.dart (2): gate at 9/10 + frozen-gate
  tripwire, controller feeds the real unlock count.
- runemark_test's 'last' pin retired to an INDEX pin (append-only
  contract — same shape as every other order list).
- vistaUnlockedFor signature grew: four test files' call sites
  updated mechanically (charsUnlocked: 0).
- Plates (tool/tenthfire_visual_test.dart): map wearing Tenthfire
  vs emberlight identity control, shelf card locked + worn.
