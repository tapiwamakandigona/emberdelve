# v0.164.0 — The Amethyst Vein

The won company's vista.

- **New vista: Amethyst** — vein-purple crystal grade for the whole delve
  (hue -60°, sat ×1.35, val ×0.96, violet wash).
- **Unlock**: win a delve with twelve different delvers. Fed by the
  junk-proofed `delvers_cleared` count — only real roster ids with a banked
  win count, so a hand-edited save can never open it early.
- Gate FROZEN at twelve: when a thirteenth delver joins, Amethyst stays
  earned (v0.100 lesson — milestones freeze, never re-lock).
- `vistaUnlockedFor` gains a `delversCleared` input; controller feeds it
  from `statValue(meta, 'delvers_cleared')`.
- Pins: `test/amethyst_test.dart` (order-LAST, gate 11/12 truth table,
  freeze tripwire, controller feed, junk-key immunity); tenthfire keeps its
  seat by index in tenth_chair_test; vistasOrder.length 12 in runemark_test.
