# v0.167.0 — A Word From You

Makes the Play in-app review ask reachable (PR #98). The ask had shipped
23 Aug but its gate required a second run win — effectively dead code in
a roguelite, which is why the store listing showed no public rating
despite a 5.00 console average.

- New third trigger: climbing into Sparktender (24 marks) or above —
  reachable in one evening without ever winning a run
  (`ReviewService.rankAskFloorMarks = 24`).
- Tinderhand (8 marks) deliberately stays below the floor: reachable
  inside the first run, too early to have an opinion worth asking for.
- Charter intact: one ask ever, stamped on request, sticky through
  cloud merge, never during the Guided Delve tour, no incentives, no
  pre-filtering, official Play API only.
- Tests: 5 new cases (rank path, too-early floor, non-rank-up bank,
  never-twice / never-during-tour guards, single-stamp semantics).
