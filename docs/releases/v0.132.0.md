# v0.132.0 — The Second Mark

The first DEPTH move in the temper lane (v0.124–0.130 filled the
six axes; the lane map called for depth next):

- **Two tempers a delve**, up from one. One mark is a pick; two
  are a plan — blade+echo for a verb loop, mend+gilt for the long
  road, surge on both for the reroll build. Six runes choose-two
  is 21 pairings where there were 6 picks.
- Rest copy counts honestly: 'Two marks a delve.' → 'One mark
  left.' → the option disappears (the v7 spent rule). The sheet
  and the anvil button say the same words.

## Anchor safety (reviewed before writing a line)

- The golden bot tempers AT MOST ONCE by its own policy (==0
  check), so tempers-on test walks are unchanged and tempers-off
  goldens hold by construction — proved by sim_test in this
  release run, byte-identical.
- A cap raise is replay-permissive: no recorded command stream
  loses meaning. simVersion stays 7.

## Technical

- One-line sim change (cap 1→2 in runTemperFace); the rest is
  honest copy. test/second_mark_test.dart (4): two customs legal,
  bot walk still single-temper, tempers-off zero, counted copy
  widget-pinned through both states to disappearance.
- PLATE CATCH: the anvil button still said 'once per delve' beside
  the new counted body copy — greps missed it because the words
  differed ('once' vs 'one'). Grep for the CONCEPT's neighbors
  (per delve), not just the changed words.
