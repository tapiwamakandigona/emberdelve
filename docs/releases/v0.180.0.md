# v0.180.0 — The Spoken Badge (RELEASED 2026-09-05 — tag v0.180.0 at c7a0247)

Built under owner directive 2026-09-02d: research is over, the R-series is
the backlog, first-run teaching first. Cut on the owner's "cut releases" call
of 2026-09-05: tag v0.180.0, GitHub release with sha256 table. Play upload
stays a separate owner call.

## The Spoken Badge (first-run teaching, R9 §3)

From layer 5 the Vent Ram and Cinder Urchin bring response-puzzle intents —
**charge** (break it by dealing the threshold this turn) and **counter**
(every non-killing strike costs you). Their badge was icons and numbers; the
words that decode them lived only behind an untaught long-press or a sealed
codex entry. Now the **first time an enemy ever declares** a charge, counter,
stagger or attack+block, the existing explain call-out speaks on its own
("CHARGING 34 — DEAL 9 TO BREAK"), once per kind, then never again on any
device (keys in `tipsSeen`, union-merged in cloud). A call-out, not a card:
it fades unaided; nothing to dismiss, no scrolling, no new screen.

Quiet while a tip card, a tour beat or the how-to-play deck is up — the kind
recurs, so it speaks at its next declaration. Plain attack/block badges are
taught by the tour and never speak on their own. The how-to-play card THE
DARK FIGHTS FAIR gains one sentence: "Hold any badge to hear it in words."

A stagger is only ever declared by breaking a charge (no foe patterns one),
so its words ride the CHARGE BROKEN! call-out the first time.

`lib/sim` untouched. Tests: `test/spoken_badge_test.dart` — director
once-ever/quiet/not-consumed, allSeen contract, JSON + cloud-merge
persistence, and three widget pins (a fresh profile hears the first charge
unasked exactly once; an opening charge speaks after the stage settles;
breaking a charge speaks the stagger once).

## Review-prompt gate (R10, backlog item 2)

R10 read the in-app review path from the code: the ask could fire at the end
of a profile's **first** run — a won daily/weekly, or a strong first clear
that climbs straight into Sparktender (bot probe: about one won first run in
six). `ReviewService.eligible` now requires `runsPlayed >= 2` (banked before
the ask; abandons count as played) on every route. Second-win, won shared
delve and Sparktender climb all still ask — one run later at the earliest.
Ratings quality over count. Header comment corrected to match the code (no
separate tour-version check exists; the live-beat check does).
Tests: `test/review_service_test.dart` gains the first-run pin on both
run-one routes and the same climb asking on run two.

## The Named Lock (R5 stage D, backlog item 3 — cheapest funnel gap)

R5 found the HARD segment carried a lock and nothing else: the only way to
learn what it held was to tap it. One dim, lock-led micro line now sits under
the difficulty caption for profiles without the Forge — "The Ember Forge opens
HARD and Ascension." — and disappears the moment the Forge is open. Copy, not
a control: it opens nothing. No price in code (the Forge sheet quotes the
store's live figure). Plates: 412×915 one line; 320×568 @1.3× wraps to two
under the glyph (`tool/named_lock_visual_test.dart`).
Test: `test/named_lock_test.dart`.

## The Widened Rotation (content, backlog item 4)

Two more composed weeks join the Weekly Delve: **Lean Road** (six floors, no
shops) and **Hard March** (six floors, no rests). Vetted by bot sweep on
kindler/normal, 150 seeds each, same sweep as the reference points: baseline
106/150; Cold Quarter 110; Lean Road 110; Hard March 92. Rejected in the same
sweep: every elites_only pair (6–16/150) — elites_only alone is 7/150, the
ladder's deliberate hard rung since v0.4.4. No sim change: both pairs are
existing ids the sim already knows.

The cycle widens from six to eight **from Monday 2026-09-14**
(`widenedFromWeek`), phased so the singles sequence runs straight through the
seam: weeks before the anchor deal exactly what v0.111.0's rotation dealt, the
anchor week and the four after it match the old cycle too, and the first week
to differ is 2026-10-19 (Lean Road where Flint Week would have come round).
No "Next Monday" line ever shown becomes false. Pairs count as doubled weeks
for the Frostvein vista and the Cold Honors, as their label carries '+'.

Honors: The Full Rotation and the Weathered are promise-worded ("every rule
in the rotation"), so per the re-pricing doctrine their target moves 6 → 8
with the live legal-label set; the title tally reads "Rules taken: n of 8".
Hearth tales: the three that said "one week in six" now tell it as it is; two
new tales for Lean Road and Hard March (68 tales; Hearthgold frozen at 10).
Codex: two rule entries (`rule:lean_road`, `rule:hard_march`, 10 embers each);
Cold Quarter's lore no longer claims to be the only doubled week.
Tests: `test/widened_rotation_test.dart` (anchor = weekIndex(2026,9,14);
pre-anchor cycle unchanged over 60 weeks; post-anchor eight-slot order;
labels/names; coming-rule truth across the seam; banned-word check),
`test/written_rules_test.dart` extended.

### Daily side

Three mutator days join Today's Trials: **Cold Camps Day** (no rests — the
one single the daily never dealt), **Lean Road Day** and **Hard March Day**
(the Weekly's new pairs). Same anchor, same honesty: dates before 2026-09-14
hash over the first eleven trials exactly as v0.156.0 did, so every trial
already dealt and every "tomorrow" line already shown stays true; from that
date the whole catalog (14) is in the hash. `legacyTrialCount` /
`widenedTrialsFrom` in `lib/game/trials.dart`; tests in
`test/widened_trials_test.dart`.

## The Shorter Title (standing UI directive: less scrolling)

The title is the game's one decision screen and it scrolled on every common
phone. Measured with the shipped fonts (returning profile): scroll extent
**412×915: 113 → 0 px** (the whole title now fits); **360×800: 251 → 112 px**;
320×568: 536 → 420 px. How: the Weekly button reads "Weekly — Cold Quarter"
(one line at 360 instead of two); Provings and Delve a seed share one compact
row and the waymark keeps its own line (same keys, same destinations, 40-px
targets); vertical padding l instead of xl; three section gaps m → s and the
logotype→tagline gap s → xs. No copy other than the two labels changed; the
Provings link reads "Provings — n of 30". Pinned by
`test/shorter_title_test.dart` (0 at 412×915, ≤130 at 360×800, footer row).
Plates: `tool/named_lock_visual_test.dart` (five sizes, top and bottom).

## The Nearer Fire (scroll audit, run-end summary)

The same real-font scroll audit run across the run screens: boon fits;
combat fits; the map scrolls its layers (a content list, by design); the
**run-end summary ran 1001 px of scroll on an 800 px phone**, with the one
decision it asks for — Delve again — 1.4 screens down past stats, pool,
honors, codex page, tip, sightings, code and the two teases. 'Delve again' is
now a **pinned footer**: on screen from the summary's first frame at every
size, won or lost, same fast restart into the boon pick. Everything else keeps
its order and scrolls above it. Pinned by `test/nearer_fire_test.dart`
(320×568, 360×800, 412×915). Plates: `tool/summary_scroll_visual_test.dart`.

## The Quieter Roster (scroll audit, character list)

The delver list is a content list and may scroll — but twenty-one locked
delvers each carried a full-height disabled LOCKED button under a header that
already showed the lock and the price: a screen and a half of dead controls
on a 360×800 phone. An unaffordable lock now shows no button; 'Unlock (N
embers)' returns the moment it can do something, and unlocked delvers keep
'Delve as …'. Card height ~250 → ~185 logical px for locked rows. Long names
get breathing room before the lock. Pinned by `test/quieter_roster_test.dart`.

## The Centered Hollow (scroll audit, mid-run screens)

The same audit across the mid-run screens at 360×800 and 412×915: the boon
pick, keystone pick and reward fit without scrolling; the map scrolls its
layers and the shop its wares (content, by design); the hollow's decisions —
Move on, Temper — sit above the fold with the forge list scrolling beneath
them. One plate caught a visual slip: with dice to forge, the hollow lays out
as a stretched list and the title **A warm hollow** sat flush against the left
edge of the screen with its first glyph shaved. Centered now in both layouts.
Pinned by `test/centered_hollow_test.dart`. Plates:
`tool/midrun_scroll_visual_test.dart`.

## The Still Route (performance)

Dragging the map into its end-of-list stretch repainted the entire route every
frame — the gold/ember HUD, the footer line and the backdrop re-rasterized
sixty times a second on top of the map itself (probe: 80 paints per frame at
360×800). The cause was structural: Android's stretch-overscroll indicator
lives inside the map's `LayoutBuilder`, and every setState there scheduled a
relayout that walked up to the enclosing Column and marked paint clear to the
route boundary. The map body is now its own relayout and repaint boundary;
the same drag costs 16 paints per frame, all of them the map's own pulsing
medallions and drifting embers. Pinned by `test/still_route_test.dart`, which
counts HUD text paints during a sixty-frame drag (57 before, 0 after). The
roster, rest, shop and summary lists were measured the same way and were
already still. Probe kept at `tool/scroll_paint_probe_test.dart`.

## The Spoken Stones (content)

Three events join the deck at its end — 56 rooms now — themed to this
release's teaching line: the delve keeps its word, and every price is written
on the wall before you pay it.

- **The Fair Scale** — weigh your gold (−15 gold, +14 embers), weigh your
  wounds (heal 20%), or leave the pans empty.
- **The Two Marks** — cut your marks (−5 hp, +3 max hp), count the older marks
  (+18 gold), or pass by.
- **The First Lantern** — take the ember (−30 gold, a random relic), warm your
  hands (heal 30%), or leave it burning.

Every option is checked against what the sim does (`test/spoken_stones_test.dart`):
labels pay exactly what they say, an unaffordable option is not a command, and
events never kill. The deck is append-only: ids never reorder, so saved
codex and tale references stay valid.

### Eighth golden re-anchor (deck growth, documented procedure)

Deck growth changes the unseen-event pick, so every seeded run re-hashes
(v0.25.0 / v0.46.0 / v0.114.0 precedent). Probe:
`tool/reanchor_v1800_probe_test.dart`, every hash measured twice.

- `goldenV6` 456904381 → 490989940; per-boss goldens re-measured
  (pyre_matriarch byte-identical — its path draws no event before the boss);
  old → new recorded in `test/sim_test.dart`. All eight boss anchor seeds
  still reach their boss.
- Four provings re-seeded, each to the first winner after the old seed under
  the same rule as v0.114.0: The Runesmith's Proving 5 → 6, The Stoker's
  Proving 12 → 14, The Fifteenth Rung 14 → 20, The High Stakes 500 → 504.
  Twenty-six of thirty kept their seed. Clears already earned are untouched
  (stored by proving id).
- Test-only re-pins (loss seeds that now win): flintwright 3 → 8, peddler
  13 → 18, runesmith 3 → 18, hearthkeeper 3 → 12, hedger 3 → 5, miller
  25 → 5; kindler rest-walk 13 → 14; full-HP shop 10 → 19; A20 ladder
  ascetic 39 → 10 (kindler 20, warden 4, gambler 40 unchanged; four distinct
  seeds kept).

## The Shorter Scroll (small phones)

On 320×568-class screens the event room's 96 px scroll glyph pushed three-line
prose under the fold — the longest rooms (The First Lantern, Rival's Ledger)
scrolled 43 px while the choices stayed pinned. On short screens the glyph is
now 56 px and the gaps above it tighten; every room's prose sits whole above
the choices at 320 wide (measured with the shipped fonts). Taller screens are
unchanged. Pinned by `test/shorter_scroll_test.dart`; plates
`tool/spoken_stones_visual_test.dart`.

## The Seventh Cycle closes (hearth tales)

Two tales join the fire's book — one for the fair scale, one for the wall of
marks — so the seventh cycle of ten is whole: the six second-circle chairs,
the two widened weeks, the two Spoken Stones rooms. Seventy tales now, seven
full cycles; the Hearthgold milestone stays frozen at the first ten. Each line
states a fact the game can prove, and `test/seventh_cycle_test.dart` checks
the two room tales against the events they describe. No dice are drawn for a
tale, so nothing seeded moves.

## The Held Hand (guided tour)

A fresh-profile walk at 360×800 (`tool/fresh_walk_visual_test.dart`) showed
END TURN lit and tappable under the tour's "PICK ONE UP" card: one stray tap
handed the enemy a free turn while the card kept asking for a die. On the
tour's action beats only the spotlit anchor is live now — the four bands
around the hole swallow taps; SKIP stays on top and always works; with no
anchor laid out nothing is held, so the tour can never trap a player.
`test/held_hand_test.dart` taps END TURN under the pick beat and checks the
turn counter does not move (red without the bands, green with them).

## The Quiet First Delve (song credits)

The same walk showed the once-ever song-credit toast ("Into the Delve" —
first hearing) landing on the very first decision screen, over the boon
skip button, before the player knew what a delve was. A brand-new profile's
first delve is quiet now: its four new songs still bank and the summary's
grouped new-song line still names them; per-song credits resume from the
second delve. `test/song_credit_test.dart` pins both halves.

## The Whole Word (320 px phones)

The fresh-profile walk re-run at 320×568 (the smallest phones in the
installed audience) showed the ATTACK button reading "ATTAC / K": the
ATTACK / BLOCK pair shares one row, each button got 138 px, and the xl side
padding left 90 px for icon + label. On screens under 340 px wide every
button keeps l side padding instead; nothing else moves, and 360 px and up
are pixel-identical. The action zone also gets one line of height back on
those phones. `test/whole_word_test.dart` measures both labels with the
shipped Cinzel at 320 and 360 and requires one line each. The plate tool
takes `PLATE_W`/`PLATE_H` so the same walk can be re-plated at any size.

## The Named Deaths (fair-death insights)

The five bosses added since v0.22.0 — Cinder Hierophant, The Bellows,
Ashfall Twins, Slag Regent, Hearthless King — had no death-screen bucket of
their own and fell through to the generic boss lines. A Bellows death was
coached with "never swing into a raised guard", which against the Bellows is
every turn. Each now has three honest, number-free lines built from its
actual pattern and the sim's block timing (a shown guard walls the *next*
player turn; the first turn always lands bare). `test/named_deaths_test.dart`
pins every claim to the pattern it describes, so a boss re-tune trips the
line that would start lying. Same three-line draw shape, no RNG consumed;
one boss golden (`slag_regent`, whose anchor run dies to the Regent) re-pins
because the run_lost event hashes the insight text — documented in
`test/sim_test.dart`.

## The Black Glass (thirteenth vista)

A new background for the "change backgrounds" ask, earned not sold:
**Obsidian** unlocks once eight different bosses have been put down — the
bestiary's own vista. Dark volcanic glass with its green-black sheen: the
embers swing to cold jade, saturation drains, and the delve sits a shade
darker. The first cut read as a second Deepshale on the plates and was
re-graded; the shipped grade is plated against Deepshale, Frostvein and
Verdigris at surface and depth. The gate is FROZEN at eight (milestones
freeze) and junk-proofed: only ids that are real bosses in the bestiary
count, so an edited save can never open it early. Appended last in
`vistasOrder`; `test/black_glass_test.dart` pins the gate, the freeze
tripwire (a ninth boss must not move it), the junk-proofing and the older
gates' unchanged call shape.

## The Still Glow (performance, reduce motion)

An idle paint census of every screen (title 3.0, rest 1.0, shop 0.0,
summary 1.0, character 3.0, combat 1.0, map 9.0 paints/frame) named the
map's cost: four `_MedallionPainter` repaints per frame from the
reachable-node halo, breathing on a repeating controller that never asked
Reduce Motion — the only repeating clock in the UI that did not (drift,
logo, sprites and weapon sway all stop). Under reduce the halo now holds at
mid glow: reachable nodes still read as lit, and the medallions stop
repainting. `test/still_glow_test.dart` counts medallion paints over sixty
idle frames: 0 with motion reduced, >0 with motion on, and the flip takes
effect mid-screen both ways. Motion on is unchanged.

## The Wayside (codex place)

The map's question-mark rooms — the one node kind a new delver cannot
guess from its icon — had no words of their own in the Codex. **The
Wayside** joins the world shelf (nine places now), priced at 10 like every
place, shelved last among them. Fiction only: it names what such rooms can
hold (a scale, a shrine, a lantern) without pricing any of it; what a given
room offers stays free on its own screen. Codex 127 → 128.
`test/wayside_test.dart` pins the price, the shelf position, the name and
that the rooms it alludes to exist in `lib/data/events.dart`. Plated on the
world shelf at 390×844 (`tool/codex_lanes_visual_test.dart`, which gained a
`scrollTo:` entry option).

## The Honest Count (hearth tale 5)

The fifth hearth tale — the first thing the fire says about the company —
still said "Sixteen keep this fire" and named sixteen, while the delver
picker one screen away shows twenty-two chairs under two circle headers.
The v0.120.0 rule already covers this: position 5 must never state a count
the picker disproves. It now reads "Twenty-two keep this fire, in two
circles: the founding sixteen, kindler to hearthkeeper, and the six who
drew the second. Each answers the delve." The later "Sixteen chairs" tales
speak of the first circle and stay as published. `test/honest_count_test.dart`
ties the tale's number word to `characters.length`, so a twenty-third chair
trips the tale, not a player. Tales draw no RNG; nothing re-anchors.

## The Many-Handed, honestly (hearth tale)

Same sweep, one more: the Many-Handed tale said "Sixteen ways down, and the
name asks all of them", while the honor itself moved to the live roster in
v0.179.0 and asks twenty-two. Re-told; `honest_count_test` now also ties the
tale's number to the epithet's target.

## The Eighth Cycle (hearth tales 71–80)

Ten more tales, 80 = 8 × the frozen Hearthgold milestone: the three
ungoverned crowns by their habits (Tyrant's four beats, Matriarch's three
rising blows, Colossus's opening shield), the black glass, the first
lantern, the shared daily road, the spoken badge, the short road (six
floors against nine), the held hand (five steps, roll to reroll), and the
count. Every line states a fact the game can prove;
`test/eighth_cycle_test.dart` couples each to its data (boss patterns,
`vistaUnlockedFor('obsidian')`, the lantern's options, `dailySeed`,
`shortRoadCfg.layers`, `TourBeats.all`). Cycle count pins 70 → 80 in the
fourth/fifth/sixth/seventh cycle tests and `hearth_tale_test`; the seventh's
"wraps after 70" pin now reads "runs on into the eighth". Tales draw no
RNG; nothing re-anchors. News line three mentions the tales.

## The Single Word (hollow)

The hollow's temper button read "Temper a face — two per delve" (or "— one
mark left") and wrapped to two lines at 360 and 320, repeating the count the
sentence above it already states ("Two marks a delve." / "One mark left.").
The count is now said once, in the sentence; the button says only what it
does — "Temper a face" — and stays one line on every phone. The Forge shelf
climbs about 46 px on the smallest screens. `test/single_word_test.dart`
measures one line with the shipped Cinzel at 320, 360 and 412;
`second_mark_test` keeps pinning the sentence in both temper states.
Plated at 320×568 and 360×640.

## The Kept Line (buttons)

A probe of every full-width button label with the shipped Cinzel found two
more two-liners on small phones: the hollow's "Rest — heal 13 HP (10 to 23)"
at 320, and the Forge's "Kindle the Forge — 4,99 US$" at 360 under Play's
longer locale price formats. Both carry a leading glyph. `EmberButton` now
measures its label against the width a full-width button has on this screen;
when the words would not fit beside the glyph, the glyph steps aside and the
words keep their one line. Short labels (ATTACK, BLOCK, "Skip — delve
unaided") keep their glyph everywhere; nothing moves on phones where the
label already fit. `test/kept_line_test.dart` pins the three long labels at
320 and 360 and the glyph on short ones. Plated at 320×568: the Forge shelf
is now above the fold.

### The Kept Line, cached (performance)

The fit question ("does this label sit on one line beside its glyph?") is
answered by a text layout. It depends only on label, padding, text scale and
room — never on color or press state — so it is now laid out once per
distinct question and remembered (bounded cache), instead of on every
rebuild. A pressed button rebuilds twice; the combat bar's buttons rebuild
with every hand refresh. Behavior identical; the existing 320/360 tests
cover the cache key (same label, different room, different answer).

## The Fallen Crown (won screen)

A win said "The Ember is yours" and the delver's name, and never once named
what had just been put down — while the death screen has spoken in every
boss's own words since v0.163. Now one italic sentence under the delver's
name answers the fallen boss's Codex entry: the Tyrant's unpaid taxes, the
Colossus told where to stop, the Bellows' empty office, the Twins' jury, the
Regent's causeway, the King's stopped clock. `lib/data/crowns.dart`, chosen
by `bossForSeed(runSeed)` — no RNG, nothing re-anchors. Lines ≤ 110
characters (four lines at 320); number words are pinned to the boss patterns
they describe; every boss and only bosses have one; the lost screen shows
none (`test/fallen_crown_test.dart`). Plated at 360×800 and 320×568
(`tool/fallen_crown_visual_test.dart`).

## The Set Stones (codex)

The Codex covered enemies, relics, delvers, places, rules, marks and dice —
and never the four keystones, the one run-long rule a delver sets after the
first won fight. Four entries now close the tools of the trade in their own
lane, THE KEYSTONES, between THE MARKS and THE DICE (eight lanes; the book
is 132 entries). Each answers its keystone's rule without restating the
numbers: the edge not yet swung, the wall that remembers, the court that
paid for variety, the two bellows worked in turn. Priced 15 like dice and
marks. `test/set_stones_test.dart` pins the entries against `keystonesOrder`,
the price, the banned-word sweep, the standard buy flow, and the lane chip
walking to the header; `codex_lanes_test` counts eight chips. Plated at
390×844 and 320×568 with two of four unsealed (`codex_lanes_visual_test`,
whose scroll-to budget grew to reach the book's last shelves). Codex counts
in the ledger and summary are dynamic; nothing else moves.

## The Open Book (title)

Every tester who left words asked for a manual. The how-to-play deck has
existed since v0.3.10 — five cards, skippable, tap-anywhere — but its only
door was a small "?" inside a fight. The title's top row now carries the
same "?" beside the Ledger and Settings glyphs; it opens the deck centered
on its own screen (`PrimerScreen`), before the first delve, and costs the
fold nothing. A first draft put a footer link beside "Provings · Delve a
seed"; plates at 320 and 360 showed it wrapping onto its own line with an
orphaned dot, so it moved up. Reading the manual changes no saved state:
the guided first fight and the context tips run exactly as before
(`test/open_book_test.dart` pins link, first card, Next through every card
back to the title, Skip, and untouched `tutorialSeen`/`tourSeenVersion`).
Plated at 320×568 and 360×800 (`tool/open_book_visual_test.dart`).

## The Read Page (how-to-play deck)

Re-read the five cards against the game they describe. Four hold (half the
embers kept on a fall; pair +2, triple ignites, straight = free risky reroll;
block resets each turn; roll → pick → spend). Card two promised the badge is
"always attack, block, or both" — three deep foes have carried charge and
counter intents since v0.47.0. It now names them and points at the badge
that speaks the first time you meet one; the matching context tip gained the
"hold to hear it" line. While plating the deck the way a fight shows it, a
pre-existing bug surfaced: the lean that sat card one low (120 px of fixed
padding) and card two high (210 px) overflowed on small phones — measured
with the shipped fonts, card one by 43 px at 320×568 before any text change,
and more after it. The lean is now flex spacers that yield when the card
needs the room. `test/read_page_test.dart` opens the deck from the fight's
own "?" at 320×568, 360×640 and 360×800 with real fonts and walks every card
with no layout exception (fails on the old layout); `open_book_test` pins
card two's new words against the intent kinds in `enemies.dart`. Plated in
the fight at 320 and 360 (`tool/read_page_visual_test.dart`) and in the
primer.

## The Read Page, continued (can't-overflow guarantee)

The flex lean keeps the deck inside the screen at 1.0× text; at 1.3× on a
320×568 phone a card is simply taller than the phone. The deck now has the
same guarantee the context tips have had since v0.10.0: the card is capped
at the screen and its body scrolls past that, never clips.
`read_page_test` gained the 1.3× case (real fonts, no layout exception,
every card reached).

## The Honest Road (death-screen insight)

The early-death bucket — the line a brand-new player is likeliest to read —
said "Fight the low-HP enemies first when a path branches." The map never
shows an enemy or its HP; it marks a room's kind. The line now coaches the
read the map does offer: "Every room ahead is marked on the map. A road with
a rest fire on it forgives a bad fight; a road of nothing but fights does
not." Claims checked against `map_gen`: rests carry no layer gate and
events open on floor 2, so both can sit on floors 2–3; elites start on
floor 4, so the early bucket never mentions them. Draw shape unchanged
(three lines); the full suite stayed green, so no anchored run dies to that
line. `test/honest_road_test.dart`.

## The Storied and the Lettered (two epithets + one honest line)

Two promise-worded titles for the long road, both derived from counters the
Ledger already banks, nothing new persisted:
- **the Storied** — "Hear every tale the rest fire tells." Target 80 =
  `hearthTales.length`; the fire tells its tales in order, so eighty hollows
  left means every tale heard once (the test walks the sequence and counts
  distinct tales).
- **the Lettered** — "Unseal every page of the Codex." Target 132 =
  `codexEntries.length`.
Both sit before the Proven, which stays last. Both targets are pinned to the
live catalogs.

The Bossbane's unlock line said "Fell all three bosses of the deep" — the
bestiary has held eight since v0.112. Text now reads "Fell three different
bosses of the deep"; the target stays 3 because a worn title never re-locks.
New general pin: no epithet may say "all"/"every" of a catalog its target
does not cover. `test/storied_lettered_test.dart`; news line 3 extended.

## The Long Shelf (two honors)

The Codex ladder ran 10 → 40 → (all 132, the Lettered) and the tale ladder
10 → (all 80, the Storied); both catalogs outgrew their middle. Two honest
middle rungs, recognition only, nothing granted:
- **The Long Shelf** — Unseal 100 Codex entries.
- **Fireside Fixture** — Hear 40 hearth tales at rests.
Honors 91 → 93. `test/long_shelf_test.dart` pins each rung strictly between
the rung below and the epithet above, inside the live catalog, earned at
exactly the target, ethics-swept.

## The Stall and the Book (codex, two places)

Two rooms every delver visits had no page: the Ashmonger's stall on the way
down and the Ledger at the top of the stairs. Both join the world shelf,
priced like every place (10 embers), after the Wayside.

- **The Ashmonger** — what the stall is and is not: three dice, two relics, a
  plate of rations, prices in gold — "the one coin the delve does not care
  about. Gold does not climb the stairs." Matches the stock rule exactly.
- **The Ledger** — the Hearth's book: embers counted and spent on chairs for
  new delvers, colors for the fire, cuts for the dice, pages for the Codex;
  every delve entered, won or lost; "never charges for reading."

Codex 132 → 134; places 9 → 11. The Lettered's target follows (134 — the
test pins it to `codexEntries.length`). The Long Shelf rung (100) is
unchanged. Zero RNG touched.

## The Paired Copies (summary, less scrolling)

The run-end summary stacked its copy-as-text offers full-width — "Copy delve
story" above "Copy seed challenge" (or the daily / weekly result) — two
rarely-tapped ghost buttons costing most of a small phone's height. They now
share one row under a small caption, COPY AS TEXT: **Story** and **Seed**
(or **Daily** / **Weekly**), each with the copy glyph. TalkBack still hears
the full sentence ("Copy delve story", "Copy seed challenge"); the copied
text is unchanged; a lone offer keeps its full-width, full-label form. The
pair holds one line each at 320 px with 1.3× text (real fonts, pinned by
`test/paired_copies_test.dart`; plates in `tool/paired_copies_visual_test.dart`).

## The Quiet Shell (performance, low-end)

Re-weighing the run-end summary with `tool/summary_probe_test.dart` found the
entrance choreography costing ~100–150 paints per frame for its first ~1.1 s
(the settled screen is 1). Four leaks, each traced with per-frame repaint
roots and `markNeedsLayout` stacks, each fixed at its cause:

- **The fade-through-black repainted the screen beneath it.** `PhaseSwitcher`
  now keeps the screen and the veil in permanent repaint boundaries (permanent,
  so the tree shape never changes — the v0.10-era remount bug stays fixed). A
  veil frame is now the veil. Every phase change in the game benefits.
- **A toast relaid the Scaffold and re-rasterized the whole body** — every
  "Healed 4", forge, reroll, first hearing, and invalid-move reason in combat
  did this on each of its slide frames. The Scaffold body is now one boundary.
  Combat toast: 22.2 → 12.6 paints/frame (what remains is the toast itself).
- **The Settling Count relaid the ledger on every digit change** — the
  paragraph had no tight box above it, so `text=` walked up through the
  IntrinsicHeight column to the viewport and re-rasterized every panel
  (~300 paints per changing frame). The number now sits in a box already the
  size of its final value, right-aligned, in its own boundary.
- **Rebuild-driven animations under the summary's LayoutBuilder** (the four
  smoldering verdict lines) marked it needs-layout every frame, and with
  loose width that walked 18 levels up and repainted the shell. `SizedBox.expand`
  makes the LayoutBuilder its own relayout boundary; a RepaintBoundary keeps
  its per-frame repaint off the vignette, drift and footer.

Entrance total (first ~1.1 s): 5,508 → 2,213 paints (−60%); drag 4.5 → 2.1
paints/frame; settled idle unchanged at 1.0. `test/quiet_shell_test.dart`
pins all three behaviors (toast paints nothing beneath it; the veil never
re-rasterizes the screen; the count's box is tight and exact) — each fails
on the previous code. Layout and visuals are unchanged.

## The Quiet Walk (performance, map)

The same root-tracing method (now kept as `tool/repaint_roots_probe_test.dart`,
which prints a paint timeline per phase entrance and the repaint roots of the
heaviest frames) turned to the map. The "you are here" delver walks node to
node after every encounter — and walked by re-positioning itself in the map
Stack each frame. A moving `Positioned` relays out the Stack, which repainted
the whole route for the 650 ms: ~137 paints/frame. The marker is now placed
once, at its destination, and the walk is a paint-only translation inside its
own boundary; its 30×34 box is its natural size, so the thinning shadow relays
out nothing above it.

Map → combat entrance: 2,850 → 1,029 paints over the first two seconds (the
remaining spike is the one legitimate mount frame). Pixels are unchanged —
walk plates diffed before/after at four moments: identical outside the
marker's own 20×26 px, max channel delta 8 inside it (boundary compositing).
`test/quiet_walk_test.dart` pins that the route Stack never paints while the
delver walks (red on the old code); `walked_path_test` (the hop, the shadow)
and `still_route_test` unchanged and green.

### The Quiet Shell, reward (performance)

The probe's full pass over every phase edge (boon→map, map→combat, combat→
keystone/reward, reward→map, shop, event) found one more: the reward
ceremony ran at ~110 paints/frame for its whole second. The three card flips
are rebuild-driven animations under the cards' LayoutBuilder — the same trap
as the summary — so every flip frame relaid the Column above and repainted
the HUD, the title and Skip along with the cards. The LayoutBuilder is now
its own relayout/repaint boundary (Center moved inside; pixels identical at
320/360/412, diffed). Ceremony: 5,655 → 1,645 paints (−71%). Pinned as the
fourth case in `test/quiet_shell_test.dart` (red on the old code).

