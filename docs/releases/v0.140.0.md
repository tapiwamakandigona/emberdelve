# v0.140.0 — The Kept Hearth

Two new hearth colours on the session's arc — and an honesty bug
the re-pricing audit caught in a badge that shipped in v0.3.3:

- **Anvilglow** (440) — red-hot iron. The first pass read
  identical to the default Emberglow on plates; a 440-ember colour
  must visibly differ, so the iron burns redder now.
- **Runefire** (480) — a violet flame that burns in marks, not
  tongues. 14 colours; the ladder still never descends.
- **Hearth Keeper honesty fix**: 'Own every hearth colour' has
  flipped at FOUR since v0.3.3 — while the shelf sold twelve since
  v0.4.3. A shipped lie, five months old. The target now tracks
  the live catalog (pinned to hearthThemesOrder.length by test)
  and the reader is junk-proof (only live colours count).

## Technical

- test/kept_hearth_test.dart (4): promise pin + junk resistance,
  ladder guard (non-descending overall — the historical
  frostfire/witchlight tie never re-prices — strict for the new),
  the one purchase path, honesty sweep.
- Plates: the title fire in both colours against the Emberglow
  control; Anvilglow re-graded after critique.
