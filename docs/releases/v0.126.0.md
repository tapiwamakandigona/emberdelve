# v0.126.0 — The Forgelight

The ninth vista, closing the temper arc's trilogy (mechanic →
honors → window): the delve lit from below, iron-red working-forge
heat — nothing like hearthgold's amber comfort or deepshale's warm
dark.

- **Forgelight**: hue -25 / sat 1.5 / val 1.12 / wash 0x66E8571F.
  First pass (-18/1.3/1.04/0x3B) was near-invisible against the
  identity control — the frostvein 'too timid' lesson repeats, and
  is now a pattern: START grades bolder than feels safe.
- Unlocks at ten tempered faces — fed by the monotonic tempersSet
  counter (banked win or lose, never derivable, never re-locks).
- Earned by delving, never sold (the vista charter, unchanged).

## Technical

- vistaUnlockedFor gains required tempersSet (16 call sites: the
  controller gate + 15 test sites patched by scripted edit).
- frostvein_test's 'last in catalog' pin rewritten to an INDEX pin
  — the same 'last' contract bug as tinker_test (v0.118.0), now
  fixed at its second occurrence; the contract is append-order
  stability, not final position.
- test/forgelight_test.dart (4): append slot + real-grade pin,
  resolver flip at exactly 10, live controller gate, honesty sweep.
- Plates: forgelight vs emberlight control maps, locked shelf card
  (unlock line), worn card. Approved after one grade strengthening.
