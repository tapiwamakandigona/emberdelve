# v0.129.0 — The Earned Titles

The temper and rotation arcs, wearable. Epithets name the player's
own history; the two newest histories had no name.

- **the Tempered** — ten marked faces. Shares the forgelight rung:
  one milestone, three rewards (vista, badge road, title).
- **the Weathered** — a Weekly won under every rule in the
  rotation. Promise-worded, so the target tracks the live rotation
  (pinned to legalRuleLabels().length). 15 epithets.
- Slotted before 'the Proven', which keeps the last word — that
  pin is a REAL contract (the Provings summit closes the shelf by
  design), unlike the three catalog 'last' pins fixed this session.

## Technical

- Zero new resolver code: epithets read the same stat vocabulary
  the honors read (tempers_set, weekly_rules_won) through the same
  statValue — the v0.127.0 junk-proofing is inherited for free.
- test/earned_titles_test.dart (4): slot indices + summit pin,
  flip at the rung, live-rotation target with junk-resistance,
  honesty sweep. deep_wardrobe's tail pin rewritten to index pins.
- Plates titles_360x640 / titles_320x568 approved (Tempered WORN,
  Weathered locked with its line, Proven closing the shelf).
