name: emberdelve
description: "Emberdelve — turn-based dice-builder roguelite (Tsoro Studios). Flutter port; deterministic Dart sim core."
publish_to: "none"
version: 0.2.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
